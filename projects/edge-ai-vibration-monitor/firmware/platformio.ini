; PlatformIO project configuration — Edge AI Vibration Monitor (v1, ESP32 dev board)
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Libraries to be pinned as the implementation lands, e.g.:
; lib_deps =
;   adafruit/Adafruit ADXL345
;   kosme/arduinoFFT   ; NOTE: on-device runtime optimisation ONLY, used after the
;                      ; hand-built FFT (ADR-0003) exists and is cross-checked against it.
;                      ; The hand-built version is the evidence of understanding; this is speed.
