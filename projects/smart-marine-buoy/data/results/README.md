# Results

Committed outputs, not just screenshots:

- **Model** — Edge Impulse exports: confusion matrix, per-class accuracy, model size, RAM/latency on device.
- **Feature study** — raw time-series vs spectral features comparison (ADR-0003).
- **Characterisation** — static-device noise-floor PSD (M1); instrument transfer function from the oscillation rig (gain/phase vs frequency); hull free-decay fit (natural frequency + damping).
- **Validation** — synthetic-vs-real distribution comparison against Waverider records (ADR-0002).
